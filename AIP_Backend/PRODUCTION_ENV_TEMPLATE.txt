# PRODUCTION CONFIGURATION REFERENCE
# Use Azure App Service environment variables / connection strings for production.
# Keep local-only secrets in an ignored appsettings.Local.json file instead.

# Azure App Service database setting
# Name: ConnectionStrings__DefaultConnection
# Value:
# Server=tcp:<azure-sql-server>.database.windows.net,1433;Initial Catalog=COOP;Persist Security Info=False;User ID=<sql-user>;Password=<sql-password>;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;

# Local development override example (store in ignored appsettings.Local.json)
# {
#   "ConnectionStrings": {
#     "DefaultConnection": "Server=localhost;Database=COOP;User Id=<local-user>;Password=<local-password>;TrustServerCertificate=True;Encrypt=True;"
#   }
# }

# JWT Configuration (MUST be 64+ characters for production)
JWT_SECRET_KEY=REPLACE_WITH_CRYPTOGRAPHICALLY_SECURE_RANDOM_STRING_AT_LEAST_64_CHARACTERS
# Optional JWT rollover support (comma-separated retired keys accepted for validation only)
# JWT__PREVIOUSKEYSCSV=OLD_KEY_1,OLD_KEY_2

# SMTP Configuration
SMTP_HOST=mail.advantage1.co.uk
SMTP_PORT=25
SMTP_USERNAME=noreply@advantage1.co.uk
SMTP_PASSWORD=YOUR_SMTP_PASSWORD_HERE

# Azure Storage (if different from appsettings)
AZURE_STORAGE_CONNECTION_STRING=YOUR_AZURE_STORAGE_CONNECTION_STRING

# Production Domain
PRODUCTION_DOMAIN=https://api.yourdomain.com
FRONTEND_URL=https://www.dibangops.com,https://dibangops.com

# Host header allow-list (comma-separated in App Service: use semicolon if required by platform)
ALLOWEDHOSTS=api.yourdomain.com;www.dibangops.com;dibangops.com

# Auth cookies (production)
AUTH__COOKIES__SECURE=true
AUTH__COOKIES__EXPOSETOKENSINRESPONSE=false

# Security hardening toggles
SECURITY__ENABLESWAGGERINPRODUCTION=false
SECURITY__ENABLESWAGGERUIINPRODUCTION=false
SECURITY__ALLOWWILDCARDVERCELORIGINS=false
SECURITY__ENABLERATELIMITING=true
SECURITY__GLOBALRATELIMITPERMITLIMIT=120
SECURITY__GLOBALRATELIMITWINDOWSECONDS=60
SECURITY__ENABLEUPLOADSSTATICFILESINPRODUCTION=false
SECURITY__REQUIREAUTHFORUPLOADS=true
SECURITY__RUNMIGRATIONSONSTARTUP=true
SECURITY__RUNPAGEACCESSINITIALIZATIONONSTARTUP=true
SECURITY__MAXMULTIPARTBODYLENGTHBYTES=10485760

# File upload hardening
FILESTORAGE__MAXFILESIZEBYTES=10485760
